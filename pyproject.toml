[project]
name = "model-distillation-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "accelerate>=1.13.0",
    "datasets>=4.8.5",
    "matplotlib>=3.10.9",
    "numpy>=2.4.4",
    "peft>=0.19.1",
    "torch>=2.7",
    "tqdm>=4.67.3",
    "transformers>=5.8.0",
    "trl>=1.4.0",
]

[[tool.uv.index]]
name = "pytorch-cu128"
url = "https://download.pytorch.org/whl/cu128"
explicit = true

[tool.uv.sources]
torch = { index = "pytorch-cu128" }
